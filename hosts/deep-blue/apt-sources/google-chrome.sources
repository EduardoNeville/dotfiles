# Google Chrome — official vendor repo (Tier 2).
# Keyring shipped by the chrome .deb itself (never curl|bash).
Types: deb
URIs: https://dl.google.com/linux/chrome-stable/deb/
Suites: stable
Components: main
Architectures: amd64
Signed-By: /usr/share/keyrings/google-chrome.gpg