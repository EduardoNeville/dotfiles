# clipmenu — clipboard picker for dwm (dwm config.h: mod+shift+v, rofi launcher).
# C rewrite lives on `develop` (master is the frozen bash version that needs
# the extra clipnotify dep) — no tags on the C line yet, branch-pin it.
# Plain Makefile (PREFIX-aware): bins + man + systemd user unit.
# desktop-only: needs X11 headers absent from the base profile.
pkg_name=clipmenu
pkg_version="develop (C rewrite)"
pkg_type=git
pkg_src_dir=clipmenu
pkg_url=https://github.com/cdown/clipmenu
pkg_branch=develop
pkg_build=make
pkg_prefix=clipmenu
pkg_bin=clipmenu
pkg_deps=()
pkg_profiles="desktop"