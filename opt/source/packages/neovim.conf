# neovim — modern Vim (git source, CMake build). Pinned to latest release tag
# (2026-09: v0.12.5). Deliberate: master is 0.13-dev rolling — update.sh is
# the explicit bump path.
pkg_name=neovim
pkg_version=v0.12.5
pkg_type=git
pkg_src_dir=neovim
pkg_url=https://github.com/neovim/neovim
pkg_branch=v0.12.5
pkg_tarball=
pkg_build=make
pkg_prefix=nvim
pkg_bin=nvim
pkg_deps=()
pkg_cmake_args=()